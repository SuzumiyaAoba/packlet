;;; packlet-test.el --- Aggregated tests for packlet -*- lexical-binding: t; -*-

;; SPDX-License-Identifier: MIT

;;; Commentary:

;; Entry point that loads the split `packlet' ERT suites.

;;; Code:

(require 'packlet-test-parse)
(require 'packlet-test-source)
(require 'packlet-test-loading)
(require 'packlet-test-autoload)
(require 'packlet-test-bindings)
(require 'packlet-test-keywords)
(require 'packlet-test-hooks)

(provide 'packlet-test)

;;; packlet-test.el ends here
